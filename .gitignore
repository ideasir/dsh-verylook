node_modules/
*.log
.DS_Store
# 开发日记：内部资料，绝不推送/发布
DEVLOG.md
*.tgz
# 运行时临时产物（绝不提交）
tmp-worker-out/
.uploads/
looklook-asr/

# 构建产物（git rm --cached 掉）
lib/
# 例外：typert.host.js 是手写路由清单（build 不生成），必须入库
!lib/typert.host.js
!lib/typert.host.d.ts
